import { GetterTree } from "vuex";
import { IcanvasState } from "./models";
import { RootState } from "../RootState";

export const getters: GetterTree<IcanvasState, RootState> = {
  // state: state => {
  //   return state.state;
  // },
};
